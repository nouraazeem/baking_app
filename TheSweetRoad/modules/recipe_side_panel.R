# UI inputs for side panel

# metric module ----
recipe_side_panel_ui <- function(id) {
  #     #align = c("l", rep("r", NCOL(DF) - 1)),
  #     list(
  #       `Recipe Name` = color_tile("white", "pink"),
  #       `Amount` = color_bar("#FA614B"),
  #       `Ingredient` = color_bar("lightblue")
  #     )
  #   )  %>%
  #     as.datatable(escape = FALSE,
  #                  options = list(scrollX = TRUE),
  #                  rownames = FALSE)
  #
  
  
  
}

recipe_side_panel_server <- function(input, output, session) {
  
  
  
}